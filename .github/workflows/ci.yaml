name: CI

on: [push, pull_request]

jobs:
  direct:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive

      - name: Build
        run:  make

      - name: Run tests
        run: |
          export REF_PATH=':'
          make test
